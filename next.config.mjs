/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: "https",
        hostname: "ajqedonopcsqsgovelij.supabase.co",
      },
    ],
  },
};

export default nextConfig;